# BiRefNet Background Removal Worker

# RunPod SDK
runpod>=1.6.0

# rembg (CPU version for build, GPU runtime will use CUDA)
rembg>=2.0.50

# ONNX Runtime (CPU for build compatibility)
onnxruntime>=1.16.0

# Image handling
Pillow>=10.0.0
requests>=2.28.0

# Required by rembg
numpy>=1.24.0
opencv-python-headless>=4.8.0
pooch>=1.6.0
